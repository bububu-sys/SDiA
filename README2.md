**Тема 1.2. Структуры данных. Работа в VS Code со структурами данных
  «мультисписок/очередь/дек/приоритетная очередь»**
-----------------------------------------------------------------------------------------------
* **Мультисписок-** 
это структура данных, которая позволяет упростить организацию нескольких списков с одинаковыми элементами. Мультисписок состоит из элементов с таким числом указателей, которое позволяет организовать их одновременно в виде нескольких различных списков. За счёт отсутствия дублирования данных память используется более рационально.

* **Очередь-**
это структура данных, которая обеспечивает FIFO-порядок элементов: чем раньше был добавлен элемент в коллекцию, тем раньше он из неё удаляется. Это стандартная модель однонаправленной очереди. Бывают и двунаправленные очереди, в которых можно добавить элемент не только в начало, но и в конец, и удалить не только из конца, но и из начала.

* **Дек-**
это упорядоченная последовательность элементов, в которой включение и исключение элемента могут выполняться в обоих концах. Дек является обобщением очереди и стека. Назначение дека — переупорядочивание поступающих данных перед обработкой.

* **Приоритетная очередь-**
это коллекция элементов, где каждый элемент имеет связанный с ним приоритет. Элемент с высшим приоритетом будет обрабатываться раньше, чем элементы с более низким приоритетом. В приоритетной очереди указывается приоритет поступающего элемента. При удалении из очереди выбирается элемент с наивысшим приоритетом (или один из таких элементов), а не тот, который поступил первым.

_____________________________________________________________________________________________

**Работа со структурами данных
  «мультисписок/очередь/дек/приоритетная очередь» на Python**
----------------------------------------------------------------------------------------
  1. **Мультисписки**

     Анализ:

     -Создаётся список списков groups.
     
     -Создаётся пустой список names.

     -В цикле внешний for проходит по каждому вложенному списку (group) внутри groups.

     -Внутренний цикл for перебирает имена внутри каждого вложенного списка,
      и добавляет каждое имя в names с помощью метода append.

     В итоге получаем плоский список всех имен из вложенных списков.

      Временная сложность: 1
-----------------------------------------------------------------------------------------------
  2. **Очередь**

     Анализ:

     -Импортируется класс deque из модуля collections.

     -Создаётся очередь tasks.

     -В очередь добавляются элементы ("task1", "task2", "task3") с помощью метода append (добавление в конец).

     -Пока очередь не пуста (while tasks: — условие, что очередь содержит элементы), извлекается первый элемент с        помощью popleft() (удаляет и возвращает первый элемент).

     -Каждое извлечение — это обработка элементов по порядку FIFO (первый зашёл — первый вышел).

     Временная сложность:
---------------------------------------------------------------------------------------     
   3. **Дек**

      Анализ:

      -Импортируется класс deque.

      -Создаётся двой-ended очередь deck.

      -Метод append() добавляет элемент в конец — добавляем "left".

      -Метод appendleft() добавляет элемент в начало — добавляем "front".

      -Метод append() снова добавляет "back" в конец.

      -В конце выводим список deck

      Временная сложность:
----------------------------------------------------------------------------------------------    
   4. **Приоритетная очередь**

      Анализ:

      -Импортируется класс PriorityQueue из модуля queue.

      -Создаётся пустая приоритетная очередь pq.

      -В очередь добавляются кортежи (приоритет, значение). Чем меньше число, тем приоритет выше.

      -Метод put() вставляет элемент со заданным приоритетом.

      -Пока очередь не пуста, извлекается элемент с помощью get(), который возвращает элемент с наименьшим                числовым приоритетом.

      -В данном случае — сначала извлекается (1, 'high-priority item'), затем (2, 'mid-priority item'), затем (3,         'low-priority item').

      Временная сложность:
______________________________________________________________________________________________________________
**Работа со структурами данных
  «мультисписок/очередь/дек/приоритетная очередь» на Java**
----------------------------------------------------------------------------------------
  1. **Мультисписки**

     Анализ:


     Временная сложность: 1
-----------------------------------------------------------------------------------------------
  2. **Очередь**

     Анализ:

     Временная сложность:
---------------------------------------------------------------------------------------     
   3. **Дек**

      Анализ:

      Временная сложность:
----------------------------------------------------------------------------------------------    
   4. **Приоритетная очередь**

      Анализ:

      Временная сложность:
______________________________________________________________________________________________________________
**Работа со структурами данных
  «мультисписок/очередь/дек/приоритетная очередь» на C++**
----------------------------------------------------------------------------------------
  1. **Мультисписки**

     Анализ:

     

     Временная сложность: 1
-----------------------------------------------------------------------------------------------
  2. **Очередь**

     Анализ:

     Временная сложность:
---------------------------------------------------------------------------------------     
   3. **Дек**

      Анализ:

      Временная сложность:
----------------------------------------------------------------------------------------------    
   4. **Приоритетная очередь**

      Анализ:

      Временная сложность:
















